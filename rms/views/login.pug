<!DOCTYPE html>
html(lang="en")
    head
    title Login
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    link(rel="preconnect" href="https://fonts.gstatic.com")
    link(href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300&display=swap" rel="stylesheet")
    link(href="stylesheets/style.css" rel="stylesheet")
    body
        h1 Welcome to Restaurant Management System
        form.formLogin(method="POST",action='')
            input#username(type="text",placeholder='username',name='username')
            input#password(type="password",placeholder='password',name='password')
            button(type='submit') Login
        form(style='text-align:center',action="/signup")
            p Don't have an account?
            button(class="btn",type="submit") SignUp
        div(style='text-align:center')
            if errors
                ul
                    for error in errors
                        li {error.msg}
            p #{message}


